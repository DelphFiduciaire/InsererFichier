{% extends 'base.html.twig' %}

{% block title %}FichierBilan index{% endblock %}

{% block body %}
    <h1>FichierBilan index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_fihcier_bilan</th>
                <th>Verif_bilan</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fichier_bilan in fichier_bilans %}
            <tr>
                <td>{{ fichier_bilan.id }}</td>
                <td>{{ fichier_bilan.nomFihcierBilan }}</td>
                <td>{{ fichier_bilan.verifBilan ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_fichier_bilan_show', {'id': fichier_bilan.id}) }}">show</a>
                    <a href="{{ path('app_fichier_bilan_edit', {'id': fichier_bilan.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fichier_bilan_new') }}">Create new</a>
{% endblock %}
