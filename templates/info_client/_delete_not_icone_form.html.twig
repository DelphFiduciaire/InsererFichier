<form method="post" action="{{ path('app_info_client_delete', {'id': info_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_client.id) }}">
    <button class="btn btn-danger" style="margin-top: -70px; margin-left: 70px">supprimer</button>
</form>
