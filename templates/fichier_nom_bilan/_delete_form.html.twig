<form method="post" action="{{ path('app_fichier_nom_bilan_delete', {'id': fichier_nom_bilan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fichier_nom_bilan.id) }}">
    <button class="btn">Delete</button>
</form>
